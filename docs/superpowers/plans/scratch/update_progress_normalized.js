const fs = require('fs');
const path = 'docs/PROGRESS.md';
let content = fs.readFileSync(path, 'utf8');

// Normalize CRLF to LF for search
const normalized = content.replace(/\r\n/g, '\n');

const target = `## 15.07.2026 - Доработка UI/UX: Чистые действия в группах, мобильная верстка и иконки dropdown`;

const lastIndex = normalized.lastIndexOf(target);

const newContent = `## 15.07.2026 - Доработка UI/UX: Выравнивание линий таблиц, действия в группах и иконки dropdown

### Сделано

- **Исправлен сломанный (ступенчатый) нижний бордюр в таблице групп**: Причина была в том, что класс \`group-actions\` вешал \`display: flex\` прямо на ячейку \`td\`, ломая её стандартное табличное поведение \`display: table-cell\` на десктопе. В результате ячейка сжималась по высоте, и её бордюр (\`border-bottom\`) рисовался выше остальных ячеек строки. Избыточный класс перенесён на внутренний \`div.group-actions\`, а сам \`td\` возвращён в стандартный табличный режим, что дало идеально ровные бордюрные линии строк.
- **Убран лишний 3-точечный интерфейс в действиях групп**: Из колонки «Amallar» удалена избыточная и дублирующая кнопка с тремя точками (\`MoreHorizontal\`), так как переход к редактированию группы уже назначен на клик по всей строке таблицы (как на десктопе, так и на мобильных устройствах). Это снизило визуальный шум и сделало интерфейс аккуратным.
- **Премиальный стиль кнопки «Yakunlash» / «Faollashtirish»**:
  - Высота кнопки на десктопе установлена в аккуратные \`28px\` с горизонтальными отступами \`12px\` и радиусом скругления \`8px\`.
  - Из стилей убраны жесткие принудительные минимальные размеры \`40px\` от старой кнопки действий.
  - На мобильных устройствах кнопке задана удобная высота для нажатия \`32px\` и увеличенное боковое поле \`14px\`.
  - При наведении добавлен премиальный брендовый ховер: золотистая рамка (\`var(--gold)\`) и мягкая полупрозрачная подсветка фона (\`rgb(212 160 23 / 5%)\`).
- **Позиционирование статуса группы в углу мобильной карточки**: Для выравнивания информации в карточках групп (\`.groups-table\`) на мобильных экранах бейдж статуса (\`Faol\`/\`Yakunlangan\`) перенесен в верхний правый угол заголовка карточки. Для этого контейнеру заголовка \`.groups-table td:first-child\` задано \`position: relative\`, а самому бейджу \`.status-badge\` — \`position: absolute; right: 12px; top: 50%; transform: translateY(-50%)\`. Это убрало лишний перенос строк и сделало карточки чистыми и сбалансированными.
- **Вертикальное выравнивание бейджей статуса**: Текст внутри бейджа \`.status-badge\` центрирован математически ровно за счет фиксированной высоты \`20px !important\`, нулевых вертикальных полей \`padding: 0 8px !important\`, \`line-height: 1 !important\` и \`justify-content: center !important\`. Шрифт увеличен до \`10px !important\`.
- **Исправлено сжатие поиска по высоте на мобильных**: В мобильном режиме тулбар переходит в \`flex-direction: column\`, из-за чего свойство \`flex: 1\` у \$.search-field ошибочно сжимало его высоту до \`21px\`. Добавлено правило \`flex: none !important\` для \$.search-field в медиа-запросе \`@media (max-width: 760px)\`, что вернуло поисковой строке стабильную и правильную высоту \`38px\` на мобильных экранах во всех разделах.
- **Исправлено исчезновение стрелки dropdown**: Свойство \`background: var(--surface)\` в правилах специфических \`select\` заменено на \`background-color: var(--surface)\`. Также в глобальные свойства \`select\` для \`background-image\`, \`background-repeat\`, \`background-position\` и \`background-size\` добавлены правила \`!important\`. Это гарантирует наличие стрелки-галочки (dropdown arrow) у выпадающих списков во всех темах и на всех экранах.
- **Унификация высоты контролов (38px)**: Все выпадающие списки (\`select\`) в тулбарах разделов \`O‘qituvchilar\` (\`.teachers-toolbar\`), \`Kurslar\` и \`Guruhlar\` (\`.course-toolbar\`) получили фиксированную высоту \`38px\` и шрифт \`13px\` для идеального визуального соответствия и выравнивания с полями ввода поиска (\`.search-field\`).
- **Премиальный фокус селекторов**: Для всех \`select\` в тулбарах разделов \`O‘qituvchilar\`, \`Kurslar\`, \`Guruhlar\`, \`O‘quvchilar\`, \`Moliya\`, \`Xodimlar\`, \`Dars jadvali\` и \`Reyting\` добавлен эффект золотистого свечения при фокусе, аналогично полям ввода поиска (\`border-color: var(--gold)\` и мягкая тень \`box-shadow\`).
- **Увеличение шрифта поиска (13px)**: Текст внутри полей поиска (\`.search-field input\`) увеличен с \`12px\` до \`13px\` для лучшей читаемости.
- **Оптимальная ширина поиска на десктопе**: Вместо бесконечного растягивания (\`flex: 1\`) или сильного сжатия (\`max-width: 340px\`), ширина полей поиска на десктопе во всех ключевых разделах (\`O‘qituvchilar\`, \`Kurslar\`, \`Guruhlar\`, \`O‘quvchilar\`, \`Moliya\`) стандартизирована до комфортных \`400px\` (с помощью медиа-запроса \`@media (min-width: 761px)\`).
- **Вертикальное выравнивание**: Для контейнера \`.student-controls\` (раздел \`O‘quvchilar\`) добавлено выравнивание \`align-items: center\` для аккуратного позиционирования вкладок и поиска.

### Изменённые файлы

- \`apps/web/src/pages/GroupsPage.tsx\`
- \`apps/web/src/index.css\`

### Проверка

- \`corepack pnpm test\` — успешно, 43 теста.
- \`corepack pnpm build\` — успешно.
- Chrome UI QA: проверено отображение в Chrome на мобильных и десктопных разрешениях. Бордюрная линия строк ровная, избыточные три точки удалены, кнопка «Yakunlash» оформлена в аккуратном стиле с золотым ховером. Высота и выравнивание остальных элементов соответствуют требованиям ТЗ.

### Следующий шаг

- Ожидание команды пользователя.
`;

if (lastIndex !== -1) {
  const updated = normalized.substring(0, lastIndex) + newContent;
  fs.writeFileSync(path, updated, 'utf8');
  console.log('Successfully updated PROGRESS.md with line alignment fix');
} else {
  console.log('Error: Target not found');
}
