import{z}from'zod';export const attendanceStatusSchema=z.enum(['came','excused','absent']);export const attendanceRowSchema=z.object({studentId:z.string(),studentCode:z.string(),studentName:z.string(),status:attendanceStatusSchema,rating:z.number().int().min(0).max(5),homeworkDone:z.boolean(),comment:z.string(),lockedByAdmin:z.boolean()});export const attendanceSessionSchema=z.object({groupId:z.string(),groupName:z.string(),date:z.string(),rows:z.array(attendanceRowSchema)});export const attendanceResponseSchema=z.object({data:attendanceSessionSchema});export type AttendanceSession=z.infer<typeof attendanceSessionSchema>;export type AttendanceRow=z.infer<typeof attendanceRowSchema>
